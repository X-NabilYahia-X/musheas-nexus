/**
 * mock-data.ts — DEPRECATED
 * All data is now served from Supabase.
 * This file is kept only for backward compatibility during migration.
 * Remove imports of MOCK_* from any page that still references them.
 */

export const MOCK_ORDERS: never[] = [];
export const MOCK_PRODUCTS: never[] = [];
export const MOCK_SHIPPING_PROVIDERS: never[] = [];
export const MOCK_SETTINGS = {};
